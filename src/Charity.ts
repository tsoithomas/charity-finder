interface Charity {
    ein: string,
    name: string,
    description: string,
    descriptionLong?: string,
    location?: string,
    locationAddress?: string,
    logoUrl?: string,
    profileUrl: string,
    coverImageUrl?: string,
    websiteUrl?: string,
}
